name: Content Radar Telegram (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: echo "JSON-based Content Radar notification workflow is retired. Supabase is the only runtime source of truth."
